@model DevPath.Models.Area

@{
    ViewData["Title"] = "Nueva Área";
}

<h2>Nueva Área</h2>
<hr />

<div class="row">
    <div class="col-md-6">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="mb-3">
                <label asp-for="Nombre" class="form-label"></label>
                <input asp-for="Nombre" class="form-control"
                       placeholder="Ej. Desarrollo de Software" />
                <span asp-validation-for="Nombre" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Descripcion" class="form-label"></label>
                <textarea asp-for="Descripcion" class="form-control" rows="3"
                          placeholder="¿Qué abarca esta área? (opcional)"></textarea>
                <span asp-validation-for="Descripcion" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Color" class="form-label"></label>
                <div class="d-flex align-items-center gap-2">
                    <input asp-for="Color" type="color" class="form-control form-control-color"
                           title="Elige un color para identificar esta área" />
                    <span class="text-muted small">
                        Este color identifica el área en el dashboard y las listas.
                    </span>
                </div>
                <span asp-validation-for="Color" class="text-danger"></span>
            </div>

            <div class="mt-3">
                <input type="submit" value="Guardar" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
            </div>

        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
