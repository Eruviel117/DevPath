name: CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configurar .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restaurar
        run: dotnet restore DevPath.Tests/DevPath.Tests.csproj

      - name: Compilar
        run: dotnet build DevPath.Tests/DevPath.Tests.csproj --no-restore

      - name: Correr pruebas
        run: dotnet test DevPath.Tests/DevPath.Tests.csproj --no-build
